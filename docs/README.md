# docs 目录说明

这个目录保存项目文档、实现说明和自动生成的上下文材料。

## 目录结构

- `本项目实现报告/`：项目结构、数据处理、模型实现、训练流程和实验设计的中文说明。
- `pdoc文档/`：由 API 文档工具生成的 HTML 站点。
- `generated/`：面向代码审阅和其他 agent 的自动生成上下文文件。

## 使用方式

- 阅读项目背景时，优先看 `本项目实现报告/README.md`。
- 需要机器可读上下文时，优先看 `generated/CONTEXT_PACK.md`。
